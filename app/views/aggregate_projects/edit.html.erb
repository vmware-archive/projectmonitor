<%= link_to '&larr; Back to Projects'.html_safe, projects_path %>
<h1>Edit Aggregate Project</h1>

<%= error_messages_for :aggregate_project %>

<%= form_for @aggregate_project do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<div style="margin-left: 30px;">
  <%= link_to 'Remove Aggregate Project -', aggregate_project_path, :confirm => 'Are you sure?', :method => :delete, :class => "button" %>
</div>
