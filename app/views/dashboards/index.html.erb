<%# TODO: add logic to render either locations or projects %>
<ol class="projects tiles_<%= @projects.size %>">
  <%= render partial: "dashboards/project", collection: @projects.map { |p| tile_for(p) } %>
</ol>
