<h1>Editing project</h1>
<%= error_messages_for :project %>
<% form_for(:project, @project, :url => project_path, :html => {:method => :put}) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>
<%= link_to 'Back', projects_path %>