= link_to '&larr; Back to Projects'.html_safe, edit_configuration_path
%h1 Add Project
= error_messages_for :project
= form_for(@project, :as => :project, :url => projects_path) do |f|
  = render 'form', :f => f
