# Projects
pivots_nyc:
  tag: nyc
  taggable: pivots (Project)
socialitis_nyc:
  tag: nyc
  taggable: socialitis (Project)
socialitis_public:
  tag: public
  taggable: socialitis (Project)

# Aggregate Projects
empty_aggregate_sf:
  tag: sf
  taggable: empty_aggregate (AggregateProject)
internal_projects_aggregate_nyc:
  tag: nyc
  taggable: internal_projects_aggregate (AggregateProject)
disabled_nyc:
  tag: nyc
  taggable: disabled (AggregateProject)
